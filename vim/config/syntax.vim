" Speed up the Vue syntax highlighting by disabling the CSS pre-processor
" detection.
let g:vue_disable_pre_processors=1

" Only use the JSX syntax highlighting in `.jsx` files.
let g:jsx_ext_required = 1
