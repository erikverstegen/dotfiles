" Search while typing.
set incsearch

" Highlight the search matches.
set hlsearch

" Case insensitive searching.
set ignorecase

" Case-sensitive searching if the expression contains a capital letter.
set smartcase

" Hide the `.git` directory and `.DS_Store` files.
set wildignore=.git,.DS_Store
